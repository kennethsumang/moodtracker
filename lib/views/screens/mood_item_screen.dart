import 'package:flutter/material.dart';

class MoodItemScreen extends StatelessWidget {
  const MoodItemScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      mainAxisAlignment: MainAxisAlignment.start,
      children: <Widget>[
        Text("Mood Item Screen")
      ],
    );
  }
}